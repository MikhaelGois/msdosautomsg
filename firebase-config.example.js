// Firebase integration removed — example config no longer needed.
// If you need to re-enable Firebase in the future, recreate a proper
// `firebase-config.js` and a safe initializer, or restore this example.
